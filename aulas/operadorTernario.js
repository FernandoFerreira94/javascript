const player01 = 999;
// const validacao = variavel (logica ===/< / > ) comparação ? retorno de true : retorno de false
const validacao = player01 >= 999 ? "VIp" : "Não vip";

console.log(validacao);
